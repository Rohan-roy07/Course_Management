# LMS Client

This is a simple React frontend for the Learning Management System backend.

## Setup

1. Navigate to the `frontend` directory.
2. Run `npm install` to pull down dependencies.
3. Use `npm start` to start the development server (it will proxy API requests to the backend).

## Features

- Login page
- Course listing and creation form

Customization and expansion are encouraged!